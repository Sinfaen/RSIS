// Autogenerated by the RSIS Framework

extern crate modellib;
extern crate libc;

use libc::c_char;
use std::ffi::CString;

use modellib::BaseModel;
use modellib::ReflectClass;
use modellib::ReflectMember;

{{STRUCT_DEFINITIONS}}

{{CONSTRUCTOR_DEFINITIONS}}

{{REFLECT_DEFINITIONS}}

#[no_mangle]
pub extern "C" fn create_model() -> Box<Box<dyn BaseModel>> {
    Box::new(Box::new({{STRUCT_NAME}}::new()))
}
